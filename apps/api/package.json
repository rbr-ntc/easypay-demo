{
  "name": "@easypay/api",
  "version": "0.2.0",
  "private": true,
  "description": "HTTP + SSE сервер EasyPay: состояние столов, смена, кухня, зал",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@easypay/config": "*",
    "@easypay/domain": "*"
  }
}
